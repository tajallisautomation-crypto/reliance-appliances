'use client'

// SEO metadata is handled server-side via Next.js generateMetadata() in app/*/page.tsx.
// This stub keeps existing <SEO /> JSX from breaking while rendering nothing at runtime.
export default function SEO(_props: {
  title?: string; description?: string; keywords?: string;
  path?: string; ogImage?: string; noIndex?: boolean; type?: string;
}) {
  return null
}
